import styled from 'styled-components';

export const Container = styled.div`

    display: flex;
    align-items: center;
    justify-content: center;
    width: 100vw;
    height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;

`;

export const Content = styled.div`

    background: linear-gradient(-38deg, transparent, var(--bg-Shadw01)), var(--bg-Whit01);
    border-radius: 2.375rem;
    padding: 4.0625rem 1.25rem 2.5rem 1.25rem;
    border: var(--border01) 0.03125rem solid;
    box-shadow: 10px 10px 22px 5px rgba(0,0,0,0.06);

`;